{
  "name": "produce-post",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"hugo server -D\"",
    "build": "tinacms build && hugo",
    "tina:build": "tinacms build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.5.39",
    "tinacms": "^1.5.28"
  }
}